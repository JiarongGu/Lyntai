<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <PackageId>Lyntai.Providers.CodexCli</PackageId>
    <Description>Lyntai LLM provider that spawns the OpenAI `codex` CLI (`codex exec --json` JSONL over stdin/stdout, no API key) — wired via builder.AddCodexCliProvider().</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Lyntai.Core\Lyntai.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Lyntai.Tests" />
  </ItemGroup>

</Project>
