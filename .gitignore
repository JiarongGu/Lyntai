# .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
.vs/

# Packages / publish output
publish/
*.nupkg
artifacts/

# BenchmarkDotNet output
BenchmarkDotNet.Artifacts/

# API-surface approval-test diffs (the .txt baselines ARE tracked; the .actual diffs are not)
*.actual

# devtools scratch (family convention: `_`-prefixed, never OS temp)
devtools/_*
devtools/screenshots/

# Private sensitive-pattern tokens (loaded by check-sensitive at runtime; never tracked)
local/

# Subagent-driven-development scratch: one directory per plan, holding its ledger, task briefs, implementer
# reports and review packages. Working state for a run in progress, never the record — git history is that.
.superpowers/

# OS / editor
.DS_Store
Thumbs.db
